#include "main.h"
/**
 * main - program that generate random valid
 * passwords for the program 101 crackme
 * Return: always 0(success)
 */
int main(void)
{
	int pass[100];
	int i, sum, n;

	sum = 0;

	strand(time(null));

	for ( i =0; i <10; i++)
	{
		pass(i) - rand() % 78;
	sum(
			}
		}

